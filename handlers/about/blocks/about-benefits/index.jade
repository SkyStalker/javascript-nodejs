mixin about-benefits(data)
  +b.about-benefits
    +e('ul').list
      each item in data.items
        +e('li').item
          +e('h2').num=item.num
          +e('p').description!=item.text.join('<br>')
