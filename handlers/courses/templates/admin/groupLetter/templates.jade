
extends /layouts/main

block append variables
  - var sitetoolbar = true
  - var layout_main_class = "main_width-limit"
  - var layout_header_class = "main__header_center"

block append head
  !=js("coursesAdmin", {defer: true})
  !=css("coursesAdmin")

block content


  if !templates.length
    p Шаблонов нет
  else
    ul
      each template in templates
        li
          a(href="/courses/admin/group-letter-templates/edit/" + template.id)= template.title


  +e("a")(href="/courses/admin/group-letter-templates/add") Создать новый
