'use strict';

const Router = require('koa-router');

const router = module.exports = new Router();

const shareFolder = require('./lib/shareFolder');

// router.get('/oauth', require('./controllers/oauth').get);
// router.post('/oauth', require('./controllers/oauth').post);

// router.get('/oauth-redirect', require('./controllers/oauthRedirect').get);

