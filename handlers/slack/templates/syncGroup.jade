
extends /layouts/main

block append variables

  - var layout_main_class = "main_width-limit"
  - var sitetoolbar = true

block content

  each failure, slug in failures
    h2= slug

    p Not in slack:
    each email in failure
      div= email

